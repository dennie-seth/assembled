# Documented pre-T-0357 motion-class gaps for the character motion-class
# declaration sweep (T-0357, Codex review 2026-09-11 finding 2).
#
# Paths are relative to the assets/final/ root, forward slashes.
#
# T-0357 requires every character-generation output to declare a validated
# motion_class (idle/locomotion/transition/loop). Before this card, NONE of
# the committed character sidecars recorded one at all -- an absent class
# silently rode the same skip/pass path a deliberately-idle sheet legitimately
# uses in every downstream check (character.py's own T-0271/T-0340 module
# notes), so nothing ever actually required a sheet to say what it is. These
# 18 sidecars are exempted here because they predate the requirement entirely
# and their real classification was never recorded -- the same historical-gap
# shape character_arm_c_baseline.txt already documents for CHR-1's own
# fields. This list is expected to shrink to ZERO as each sheet is
# regenerated (picking up motion_class on merit) or has it genuinely
# backfilled by a dedicated follow-up card, never grown to silence a fresh
# violation.
#
# ---------------------------------------------------------------------------
# EXPLICIT INTERIM-PLACEHOLDER EXEMPTION -- @DennieSeth, 2026-09-11
# ---------------------------------------------------------------------------
# character/player_walk_sheet_hybrid.provenance.json IS listed below, by an
# explicit, reviewed decision. This supersedes this card's original position
# that it must NOT be exempted; that position was correct as a default and is
# overridden here deliberately, on the record, so PR #375 can merge.
#
# What it is: the shipped T-0266 walk, accepted as an INTERIM PLACEHOLDER for
# engine integration (T-0235 / T-0328 need a stand-in). It is not the game's
# walk and is not claimed to be.
#
# Why it would otherwise fail: it is the one sheet in this tree that is
# unambiguously a locomotion cycle, and a properly-classified recompute fails
# the real motion metrics on merit (Codex review 2026-09-11, finding 2):
#   pose IoU, per-frame        0.3935-0.6305   against a 0.70 floor
#   torso histogram distance   0.0469-0.3203   against a 0.15 cap
# That is a substantive failure, not a missing-label technicality.
#
# Why exempt rather than fix: there is nothing to fix in place -- the walk is
# being REPLACED, not repaired. Its replacement is the Tier-2 part compositor,
# T-0338. When that walk lands, this entry MUST be removed in the same change.
#
# This is the "explicit, reviewed placeholder exception for existing legacy
# assets" the Codex review sanctions (finding 1's fix). It is NOT the silent
# exempt T-0357 forbade: it is named, justified with measured numbers, tracked
# to a replacement card, and guarded by
# tests/test_character_motion_class_T0357.py, which also proves the exemption
# is path-exact and that the CI command still exits non-zero for any fresh
# failing artifact.
#
# KNOWN FOLLOW-ON (T-0359): this entry exempts only the missing-label check.
# If T-0359 labels the walk `motion_class: "locomotion"`, the pose-fidelity /
# identity-stability recompute will engage and FAIL on the numbers above, and
# there is no motion-fidelity baseline to exempt it from. T-0359 must not land
# before T-0338 replaces the walk, or CI goes red again.
character/player_walk_sheet_hybrid.provenance.json
character/player_crouch_hide_sheet_v1.provenance.json
character/player_crouch_hide_sheet_v2.provenance.json
character/player_die_sheet_v1.provenance.json
character/player_die_sheet_v2.provenance.json
character/player_idle_frame_hybrid_source_T0252.provenance.json
character/player_idle_sheet_arm_a_T0228.provenance.json
character/player_idle_sheet_arm_b_T0229.provenance.json
character/player_idle_sheet_arm_c_T0230.provenance.json
character/player_idle_sheet_chained_T0250.provenance.json
character/player_idle_sheet_hybrid_T0252.provenance.json
character/player_idle_sheet_pose_authority_T0249.provenance.json
character/player_idle_sheet_sdxl_T0218.provenance.json
character/player_idle_sheet_stage3_sdxl_T0218.provenance.json
character/player_idle_sheet_v1.provenance.json
character/player_idle_sheet_v2.provenance.json
character/player_move_sheet_v1.provenance.json
character/player_move_sheet_v2.provenance.json
character/pose_grid_stage3_T0218.provenance.json
