name: ci-gen-client-base

on:
  pull_request:
    branches: [develop, main]
    paths:
      - "tools/gen-client-base/**"
      - ".github/workflows/ci-gen-client-base.yml"
  push:
    branches: [develop, main]
    paths:
      - "tools/gen-client-base/**"
      - ".github/workflows/ci-gen-client-base.yml"

concurrency:
  group: ci-gen-client-base-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: tools/gen-client-base

jobs:
  lint-test:
    name: lint, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -e ".[dev]"

      - run: ruff check .

      - run: pytest -q
