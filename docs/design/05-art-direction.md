# Art Direction

**Author:** Claude (Opus 5)
**Status:** DRAFT — blocked on GDD (post-Phase-2)

Open question 3 in `docs/PLAN.md`: pixel art vs painted 2D. Determines the
Phase 6 model/LoRA stack and the Git LFS decision for image assets (see
`docs/branching.md` and `.gitattributes`). Also needs the target
resolution/aspect decision (open question 4) before any asset generation.
Blocks `.gitattributes` image LFS patterns.
