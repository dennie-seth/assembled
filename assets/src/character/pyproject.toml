[project]
name = "char-gen"
version = "0.1.0"
description = "Character generation spike: player idle sheet gate validation (T-0198, docs/design/13-asset-pipeline.md §3.5)"
requires-python = ">=3.12"
dependencies = [
  "pillow==12.3.0",
  "numpy==2.5.1",
]

[project.optional-dependencies]
dev = [
  "pytest==9.1.1",
  "ruff==0.14.4",
]

[project.scripts]
generate-synth-idle-sheet = "char_gen.synth_sheet:main"
generate-synth-move-sheet = "char_gen.synth_states:main_move"
generate-synth-crouch-hide-sheet = "char_gen.synth_states:main_crouch_hide"
generate-synth-die-sheet = "char_gen.synth_states:main_die"
generate-synth-watcher-idle-sheet = "char_gen.synth_entities:main_watcher_idle"
generate-synth-watcher-move-sheet = "char_gen.synth_entities:main_watcher_move"
generate-synth-watcher-trapped-sheet = "char_gen.synth_entities:main_watcher_trapped"
generate-synth-sound-idle-sheet = "char_gen.synth_entities:main_sound_idle"
generate-synth-sound-move-sheet = "char_gen.synth_entities:main_sound_move"
generate-synth-sound-trapped-sheet = "char_gen.synth_entities:main_sound_trapped"
generate-synth-still-air-idle-sheet = "char_gen.synth_entities:main_still_air_idle"
generate-synth-still-air-move-sheet = "char_gen.synth_entities:main_still_air_move"
generate-synth-still-air-trapped-sheet = "char_gen.synth_entities:main_still_air_trapped"
generate-synth-player-idle-arm-c-sheet = "char_gen.synth_entities:main_player_idle_arm_c"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
