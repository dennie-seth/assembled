# side_neutral panel attempt log (T-0339, RE-SCOPE 2026-09-11)

Regenerates ONLY the side_neutral panel via T-0351's proven attempt-19-21 recipe, unchanged. Hard cap: 3 attempts.

| Attempt | Seed | GPU seconds | Notes |
|---|---|---|---|
| 1 | 837462914 | 105.7 | REJECTED (pose clean, colour illegible). A genuine, uncropped, single-figure true side profile -- confirms T-0351's own finding. But the coat's own lighting is a soft, low-saturation gradient (specular highlight down the centre fold); the home palette has no bright/light green slot, so after descent the highlight midtones quantized almost entirely to neutral grey ramp slots, leaving only thin, fragmented dark-green edge pixels -- the same neutral-highlight-plus-green-block convention the already-promoted `player_idle_sheet_hybrid_T0252.png` also uses, but that sheet's costume has large solid dark-green blocks (chest band, boots) by design where this panel only has a continuous fabric gradient, so nothing survived as a coherent green mass. See docs/assets/evidence/T-0339/side_neutral_attempt_1_1024.png. |
| 2 | 674839205 | 66.4 | REJECTED (colour vivid, cutout shredded). The best-looking raw panel of the three -- vivid saturated green coat with real contrast against a darker undergarment/boots -- but its background is a dark vignette (near-black toward the edges), close enough in Oklab space to the coat's own shadow folds that the border-connected flood classifier ate through large parts of the figure, leaving only disconnected highlight fragments after descent (the exact "swept figure edge" failure mode `char_gen.cutout`'s own wide-border-span warning names). See docs/assets/evidence/T-0339/side_neutral_attempt_2_1024.png. |
| 3 | 521365985 | 69.1 | **PROMOTED AS BEST-OF-3, NOT AS CRITERION-CLEARING.** Uncropped single-figure true side profile, background a clean uniform near-black with no border-span warning from the cutout classifier -- the mask stayed one coherent blob and clears every mechanical floor (280 fg px, ~90% green-family index membership). **Correction (see docs/assets/evidence/T-0339/README.md): re-opened directly, the descended result does not read as "unmistakably green" -- it is a mottled olive/khaki column, the same continuous-gradient-quantizes-to-muddy-olive failure named in attempt 1's own rejection, just without attempt 1's additional washout. This row's original "ACCEPTED AND PROMOTED" language overclaimed the visual result; left uncorrected below for history, corrected here.** See docs/assets/evidence/T-0339/side_neutral_attempt_3_1024.png (source) and the committed `assets/final/character/player_profile_keyframe_hybrid_T0272.png` (descended result, best-available, not criterion-clearing). |

**Stop-and-report filed** (hard cap of 3 generation attempts spent, none of the three clears this
card's "vivid green legible at 40px" bar after descent): full root-cause analysis in
`docs/assets/evidence/T-0339/README.md`. Diagnosed as a descent-stage failure (palette quantization
for attempts 1/3, cutout background-bleed for attempt 2), not a generation-stage failure -- all
three attempts produced genuine, correctly-posed, uncropped single-figure side profiles.
