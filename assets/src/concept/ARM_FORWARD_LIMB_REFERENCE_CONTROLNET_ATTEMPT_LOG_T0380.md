# Forward-limb ControlNet img2img attempt log (T-0380)

Every attempt is recorded here whether promoted or not. Pose is supplied by an OpenPose ControlNet skeleton (`pose_rig_forward_limb_controlnet_T0380`, reused from the committed T-0351 `side_right_forward` rig), not the prompt -- see the generator module's own docstring for why (T-0355's prompt-only route was falsified). img2img from the T-0317 base, denoise/ControlNet strength tuned per attempt within this card's own 4-attempt cap.

| Attempt | Seed | Denoise | ControlNet strength/end | GPU seconds | Whole-frame green px | Centred-crop green px | Border max channel | Promoted | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 380001 | 0.78 | 1.4/1.0 | 48.1 | 48531 | 9992 | 4 | no | Strict profile, single arm, black background all correct, but denoise 0.78 was too low: the base image's own straight floor-length coat silhouette survived nearly unchanged and completely concealed the raised near leg -- no crop can show a leg pose that isn't drawn. Fixed in attempt 2 by raising denoise to 0.87 and adding an explicit coat-drape/swept-hem clause. See `docs/assets/evidence/T-0380/attempt_1_torso_leg_crop_no_leg_visible.png`. |
| 2 | 380002 | 0.87 | 1.5/1.0 | 51.1 | 70230 | 13180 | 4 | no | REVISED, was incorrectly logged as a pass and briefly promoted: profile, single goggle lens, orientation, black border, raised near leg, hood/mask/boots and coat-past-knee all correct, but a second, unrequested gloved hand/fist (with clearly rendered fingers) is visible resting at the hip, distinct from the extended near arm's own hand -- exactly the "far fist visible" defect T-0355's stop-and-report already falsified the prompt-only route over, missed on first read of the frame. This fails the "single visible arm silhouette" acceptance check. See the decisive crop `docs/assets/evidence/T-0380/attempt_2_second_hand_at_hip_crop.png`. The false promotion (`player_profile_forward_limb_reference_controlnet.png` + provenance) has been reverted -- see commit history. Root cause diagnosed and fixed for attempt 3/4: the far arm's elbow/wrist, reused verbatim from T-0351's `side_right_forward` rig ("held back close to the body," tuned for a multi-panel walk-cycle sheet), still projects far enough past the torso in a single strict-profile frame to read as a second hand. |
| 3 | 380003 | 0.87 | 1.5/1.0 | 45.1 | 2638 | 0 | 40 | no | FAIL, invalidated by a tooling bug, not the pose fix itself: intended to test the far-arm-collapsed skeleton (attempt 2 rendered a second visible hand at the hip), but `render_skeleton()` at generation time still delegated to `pose_rig_master_sheet_T0351.render_pose_skeleton`, which re-derives keypoints straight from T-0351's own module and silently discarded this card's collapse override -- the ControlNet skeleton actually uploaded for this attempt was the same un-collapsed one as attempt 2 (see `docs/assets/evidence/T-0380/attempt_3_main_1024.png`). The sampler also fought the identical-looking skeleton plus the newly strengthened prompt/negative-prompt into an unrelated failure mode (pale/blue costume, no forward limb extension, a detached floating green artifact, border max channel 40) rather than reproducing attempt 2's pose. Fixed in code before attempt 4 (see commit history: `render_skeleton` now draws this module's own `keypoints()` via `draw_pose_skeleton_cell` directly, pinned by a new bit-for-bit regression test) -- attempt 4 is the first real test of the collapsed-far-arm skeleton. |
| 4 | 380004 | 0.87 | 1.5/1.0 | 39.1 | 60279 | 6293 | 166 | no | FAIL, total sampler collapse: with the render_skeleton bug fixed (commit 479a199, the first real test of the far-arm-collapsed skeleton), the sampler produced an image bearing no resemblance to the requested figure at all -- a close-up abstraction resembling guitar tuning pegs/hardware against a dark, textured, non-black background (measured border max channel 166, far over the 16 ceiling; see the decisive corner crop `docs/assets/evidence/T-0380/attempt_4_black_background_corner_crop.png` showing visible green flecks and grey haze). See the full frame `docs/assets/evidence/T-0380/attempt_4_main_1024.png`. This is the 4th and final attempt under this card's pre-registered cap. |
