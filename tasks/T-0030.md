---
id: "T-0030"
title: "SPIKE: windows-latest runner godot-cpp + headless export feasibility"
status: "review"
priority: "P0"
phase: 3
agent: "infra"
depends_on: []
created: "2026-08-01"
---

## Context

Verify the free `windows-latest` GH-hosted runner can (a) build godot-cpp
via SCons/MSVC and (b) run `godot --headless --export-release`. Timebox 4h.
Document the result in `docs/ci-notes.md`. Gates T-0033 (client CI job).

Expectation per `docs/PLAN.md`: Godot 4's `--headless` uses the dummy
rasterizer, so 2D import + export packaging should be CPU-only and need no
GPU. If the spike disproves this, the fallback is a self-hosted runner on
the dev box (Windows service, not WSL - needs the GPU and Godot editor).

## Acceptance

- [ ] SCons/MSVC build of godot-cpp succeeds on `windows-latest`
- [ ] `godot --headless --export-release` succeeds on the same runner
- [ ] Result (pass or fail, with specifics) documented in `docs/ci-notes.md`
- [ ] If it fails: fallback plan (self-hosted Windows runner) noted as the
      path forward for T-0033
