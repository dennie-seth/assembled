---
id: "T-0094"
title: "Seed-phrase generation + token derivation"
status: "done"
priority: "P0"
phase: 4
agent: "server"
depends_on: ["T-0042"]
created: "2026-08-01"
branch: "feature/T-0094"
commit: "44f33c9f9c855646d465a9b43ed42a01d1f7b78a"
pr: "https://github.com/dennie-seth/assembled/pull/58"
---

## Context

New task from `docs/HANDOFF.md` Section 4, and the server-side half of
revised T-0066. Implements `docs/design/09-identity.md` Section 1: server
generates a seed phrase, derives the `identity.token` from it, and
**discards the phrase** - only the derived token is ever stored. Loss of
the phrase is final and by design.

## Acceptance

- [ ] `POST /v1/identity` generates a phrase, derives a token, persists
      only the token, and returns the phrase to the caller exactly once
- [ ] Test: derivation is deterministic - the same phrase always yields
      the same token
- [ ] Test: the phrase itself is never present anywhere in the database
- [ ] Test: derivation/mint endpoint is rate-limited (anti-brute-force,
      S-5 open in `docs/design/09-identity.md`)

## Validation: PASS (2026-08-05T11:48:22.348Z)

All 11 tests pass (6 SeedPhrase, 3 RateLimiter, 2 pre-existing server). clang-format --dry-run --Werror clean on all 7 changed C++ files. TDD order confirmed: test commit 4c183ec (identity_test.cpp only) precedes implementation commit a9548fe (9 files). All 3 commits carry Co-authored-by trailer. No free-text UGC surface: identity table schema has token+created_at only, no phrase column — verified by information_schema test. SOLID/DRY: three single-responsibility classes (SeedPhrase, RateLimiter, IdentityController). RAII throughout (make_unique, lock_guard). Doxygen on all public APIs. Encapsulation enforced — private members accessed via methods. Migration is idempotent (IF NOT EXISTS + IF EXISTS). All four acceptance criteria covered: POST /v1/identity mints and returns phrase once, derivation is deterministic, phrase absent from DB, rate limiting returns 429. DB-dependent integration tests correctly skip without DATABASE_URL (expected in CI without Postgres). Branch naming follows git-flow (feature/T-0094).

## PR (2026-08-05T11:48:22.348Z)

https://github.com/dennie-seth/assembled/pull/58
