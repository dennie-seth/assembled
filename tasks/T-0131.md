---
id: "T-0131"
title: "Fix check_inv8 as a reachability estimate (sim)"
status: "done"
priority: "P0"
phase: 4
agent: "server"
depends_on: []
created: "2026-08-04"
branch: "feature/T-0131"
commit: "44f0dc178f2a19ce70f70fb7b74a8e09b2ebafc9"
pr: "https://github.com/dennie-seth/assembled/pull/70"
comments: []
attachments: []
---

## Context

Round 2 found `check_inv8` sitting at roughly 93% of ticks across nearly
every sweep condition, which is not a meaningful signal -- it reads as an
anchored-count test rather than a measure of whether unique items are
actually reachable. Held-but-circulating items (still in play, being
carried toward delivery) were being counted as unreachable, which is
wrong: reachability should track expected time-to-delivery against the
identity's remaining runway before collapse, not a static count.

## Story

As a simulation author, I want `check_inv8` rewritten to estimate expected
time-to-delivery for a unique versus the identity's remaining collapse
time, instead of an anchored-count test, so that INV-8 responds to actual
collapse proximity and held-but-circulating items are not misclassified
as unreachable.

## Acceptance

- [ ] `check_inv8` estimates expected time-to-delivery for held/circulating
      uniques and compares it against the identity's remaining time before
      collapse, rather than performing a static anchored-count check
- [ ] Held-but-circulating uniques do NOT count as unreachable solely for
      being currently held
- [ ] INV-8 no longer sits flat at ~93% of ticks across sweep conditions --
      it visibly responds to collapse proximity
- [ ] A healthy pool with long runway before collapse reports near-zero
      INV-8 violations

## Validation: FAIL (2026-08-05T11:38:25.343Z)

Two issues. (1) Implementation not committed: the check_inv8 rewrite in tools/sim/src/sim/invariants.py exists only as unstaged working-tree changes — only the test commit (9570eb2) is on the branch. The implementation must be committed (as a separate commit after the tests, per TDD) for the card to be complete. (2) ruff check fails with 4 E501 errors in tests/test_invariants.py at lines 230, 249, 268, 288 — all make_held_item() calls exceed the 100-char line limit and need wrapping.

## Validation: FAIL (2026-08-05T14:56:24.151Z)

Same two issues as the previous FAIL, unchanged. (1) Implementation not committed: the check_inv8 rewrite in tools/sim/src/sim/invariants.py (37 lines changed) and the ruff E501 fixes in tools/sim/tests/test_invariants.py (15 lines changed) exist only as unstaged working-tree modifications — the branch still contains only commit 9570eb2 (the test commit). Per TDD, the implementation must be committed as a separate commit after the tests. (2) While ruff and pytest both pass against the working tree, the uncommitted state means the branch as-committed still has the old check_inv8 and the E501 violations. The implementer needs to stage and commit the implementation changes (with the Co-authored-by trailer) before this card can pass review.

## Validation: FAIL (2026-08-05T15:39:28.990Z)

ruff check fails: E501 at tools/sim/src/sim/invariants.py:114 — line is 101 chars (limit 100). The generator expression `1 for it in state.items.values() if it.type_id == itype.type_id and it.anchor is not None` needs wrapping across two lines to stay within the 100-char limit. All 111 pytest tests pass, commit order is correct (test 9570eb2 before impl 4e22650), both commits carry the Co-authored-by trailer, working tree is clean, and no free-text UGC surface was introduced. Only the ruff lint violation remains.

## Validation: PASS (2026-08-05T21:12:44.748Z)

All 111 pytest tests pass, ruff check clean, working tree clean with all implementation committed across 3 commits. TDD order verified (test 9570eb2 before impl 4e22650, lint fix 44f0dc1). All commits carry Co-authored-by trailer. check_inv8 correctly rewritten as min(world_path, held_path) reachability estimate — held items contribute via bleed timer instead of being counted as unreachable. Four test cases cover the key scenarios. No free-text UGC surface introduced. Only tools/sim/ files touched.

## PR (2026-08-05T21:12:44.748Z)

https://github.com/dennie-seth/assembled/pull/70
