---
id: "T-0099"
title: "Economy simulation harness"
status: "done"
priority: "P1"
phase: 4
agent: "server"
depends_on: []
created: "2026-08-01"
branch: null
commit: null
---

## Context

New task from `docs/HANDOFF.md` Section 4 - **the important one**. A
standalone simulation (a few hundred lines, no engine, no server -
plausibly lives under `tools/sim/` rather than inside `server/`) modelling
agents that join/play/idle/quit while items spawn/bleed/transfer/scatter,
with all four wall-clocks (`docs/design/10-time-and-progression.md`
Section 2) modelled together per `docs/design/08-invariants.md` Section 4.

**Build this now.** Its tuning parameters are blocked on @DennieSeth
(`docs/archive/OPEN-QUESTIONS.md`: T-1, T-2, E-7, E-1) but its structure is
not. Its assertions are the same predicates as T-0096's two-player test -
one definition (INV-6...9, INV-14), three consumers (unit tests,
simulation, production monitoring).

**The four clocks to model** (`10-time-and-progression.md` Section 2):

| Clock | Scope | Range | Open param |
|---|---|---|---|
| Held bleed | item in inventory | 60-90 min (≈2× run) | E-1 |
| World/escrow bleed | item at an anchor | 48-72 h | E-1 |
| Unlock decay | `(variant_id, tag)` | minutes -> long, tiered | T-3 |
| Collapse | identity | weeks | T-1 |

**Rarity model to spawn against** (`docs/design/07-items-economy.md`
Section 2, D-7) - a hard instance-count *cap*, not a drop probability:
common `k_c·P`, rare `k_r·P` with `k_r ≪ k_c`, unique a fixed small
absolute count exempt from population scaling. Supply shrinks only via
landing-probability failure on bleed under over-supply
(`07-items-economy.md` Section 4) - there is no confiscation mechanic to
model, just a probability that a bleeding instance doesn't land.

## Acceptance

- [ ] Discrete-time simulation with agents joining/playing/idling/quitting
- [ ] Items spawn against the rarity-cap model above (not a probability
      roll), bleed, transfer, scatter on quit/death, and can fail to land
      under over-supply via a landing-probability curve (E-8)
- [ ] All four wall-clocks (held bleed, world/escrow, unlock decay,
      collapse) modelled together, not tuned in isolation
- [ ] Assertions ARE INV-6, INV-7, INV-8, INV-9, INV-14 - checked
      continuously during a run, not just at the end
- [ ] Sweeps from `08-invariants.md` Section 4 runnable (P=2 sustained,
      growth, collapse, mass exodus, hoarder cohort, unique circulation,
      veteran-vs-new, population-helps, collapse-race)

## Validation: FAIL (2026-08-03T22:06:59.972Z)

Acceptance criterion #3 requires all four wall-clocks modelled together. Unlock decay (the fourth clock) is declared in config.py:58-60 (unlock_decay_tactical, unlock_decay_session, unlock_decay_unique_keyed) but never consumed by engine.py — no unlock/variant state exists in types.py, no engine method processes unlock decay, and no test exercises it. The parameters are dead config. To fix: add unlock state tracking to types.py (e.g. per-agent or per-item-type unlock records), add decay processing to engine.py's tick loop, and add test coverage in test_engine.py. Secondary: could not independently run pytest or ruff (no .venv, deps not installed) — the implementer's commit 6ee801f shows ruff was run, but test passage is unverified. Conduct (TDD order, trailers, no UGC) and the other three clocks + all five invariants + nine sweeps are clean.

## Results

The nine `08-invariants.md` §4 sweeps existed and ran (`sim = "sim.sweeps:main"`) but only
as fixed-config scenario checks — no run had ever exported a results artifact, and none
of them swept the open T-1/T-2/E-1 tuning brackets. Added `sim.run` (`python -m sim.run`,
tests in `test_run.py`) to grid-sweep those brackets at full uncompressed time scale and
export the results.

Full findings, tables, and reproduction steps: **[`tools/sim/results/RESULTS.md`](../tools/sim/results/RESULTS.md)**
(data: [`tuning_sweep.json`](../tools/sim/results/tuning_sweep.json) /
[`tuning_sweep.csv`](../tools/sim/results/tuning_sweep.csv), seed 42).

Headline: **INV-6 (rarity cap) never violated** across all 38 runs spanning both
brackets — the spawner/cap-enforcement is robust. **Held-bleed duration (E-1) is the one
parameter with real, actionable signal** — the fast end of its bracket (60–75 min)
cuts gating-type extinction under hoarder stress to ~6–9% of ticks vs. ~101–106% at the
range's slow/full end, while world-bleed duration shows no measurable effect.
**T-1 (collapse duration) and T-2 (unique-unlock decay) show no differentiating signal
anywhere in their brackets** — T-2 is currently invisible to every checked invariant, and
INV-8's per-tick violation rate stays flat (~51–53/tick) regardless of collapse duration.
Recommend holding both at their documented bracket midpoints (3 weeks / 7 days) absent a
reason to move.

### Round 2 — the four questions the sim exists to answer

Round 1 tuned parameters but never answered the questions `08-invariants.md` §4,
`15-server-ops.md` §8, and `12-tears.md` §3a actually block on. Round 2
(`feature/T-0099-round2`) extends the sim (explicit recipient-selection policy, chain-key
consumption, a population-sweep harness) and answers all four.

Full findings, tables, and reproduction steps:
**[`tools/sim/results/RESULTS-round2.md`](../tools/sim/results/RESULTS-round2.md)**
(data: [`round2_population.json`](../tools/sim/results/round2_population.json),
[`round2_dm5.json`](../tools/sim/results/round2_dm5.json),
[`round2_chainkey.json`](../tools/sim/results/round2_chainkey.json), seed 42).

Headline:
- **INV-14 (does population help?):** nuanced yes. Raw per-player throughput stays roughly
  flat across P=2→200 (does **not** accelerate with population, contra the strong form of
  the pitch), but the **INV-7 density floor** (whether the two rare gating types can
  simultaneously exist) shows a hard 100%-failure wall for P≤15 collapsing to single digits
  at P≥20 — population's real value is making completion *possible at all*, not faster.
- **DM-5 (recipient selection on bleed):** made the previously-implicit "veteran agents win
  contested pickups" policy an explicit, swappable parameter. Effect on fairness (Gini) is
  small (0.039–0.051) and counter-intuitive — the naive need-weighted "fairness" policy
  measurably *underperforms* both FIFO and uniform-random due to a lock-in dynamic.
  Recommend `random` if recipient fairness becomes a priority.
- **OPS-1 (shard population floor):** **P≈20** under current tuning (`k_c=2.0`, `k_r=0.2`,
  2 of 3 rare types gating) — below it, INV-7 fails on 100% of ticks across every seed
  tested (structurally, not probabilistically); above it, failure drops to single digits.
  Recommend a shard-population target with headroom above this hard floor (≥50).
- **Chain-key consumption:** simulated the documented "sends onward" (transfer) semantics
  against the literal-destruction alternative reading. Destructive consumption drains all 5
  seeded uniques within ~1 simulated hour and never recovers (total lifetime crossings: 5);
  the docs' already-locked transfer semantics sustains 2,000+ crossings over the same 3
  weeks from the same fixed pool. Empirically confirms the locked design choice is not just
  cleaner but load-bearing. The narrower "per-run vs. per-week" unlock-persistence question
  is **not answered** — it needs an identity-across-multiple-universes model this round's
  agent-per-run abstraction doesn't support.
