---
id: "T-0132"
title: "E-4 sweep: k_c, k_r, unique_count (sim)"
status: "done"
priority: "P0"
phase: 4
agent: "server"
depends_on: []
created: "2026-08-04"
branch: "feature/T-0132"
commit: "cbd249ac058be8824461321328c62dfe145eedbd"
pr: "https://github.com/dennie-seth/assembled/pull/72"
comments: []
attachments: []
---

## Context

Round 2 reported a candidate "P=20 floor" for the shard population, but
that number was derived without sweeping the rarity-cap constants (`k_c`,
`k_r`, `unique_count` from `07-items-economy.md` Section 2, open question
E-4) alongside population. Three seeds at P=30 in the transition zone
already showed an 8x spread (0.024 to 0.192), which means the floor is not
a stable constant -- it is a function of the unswept constants. Writing
"P=20" into `15` Section 3 as a fixed number would be premature and wrong.

## Story

As a simulation author, I want `k_c`, `k_r`, and `unique_count` swept
jointly with the population sweep, so that the shard population floor is
reported as a function of these constants rather than a single number.

## Acceptance

- [ ] `k_c`, `k_r`, and `unique_count` are swept jointly with the existing
      population sweep (not held fixed at Round 1/2 defaults)
- [ ] The population floor is reported as a function of `k_r` and
      `unique_count` (e.g. a table or surface), not a single scalar
- [ ] **The "P=20 floor" from Round 2 must NOT be written into `15`
      Section 3 until this sweep lands -- it is contingent on these
      unswept constants, and 3 seeds showed an 8x spread (0.024 to 0.192
      at P=30) in the transition zone.**

## Validation: FAIL (2026-08-05T11:41:46.235Z)

pytest: 1 failure — tests/test_run_round3.py:139 test_kc_variation_does_not_change_inv7_at_fixed_kr asserts delta < 0.15 but actual delta is 0.225 (k_c=0.5 vs k_c=4.0 at k_r=0.3, P=30, ticks=2000, seeds=3). Either the tolerance (0.15) is too tight or the test hypothesis (k_c doesn't affect INV-7) is wrong at these parameters — adjust the tolerance or test parameters to match the actual engine behavior. Additionally, the implementation file tools/sim/src/sim/run_round3.py exists on disk but is untracked (not committed), and tools/sim/pyproject.toml has an unstaged entry-point change — both must be committed for the card to be complete. ruff: clean. Commit trailer: present. No UGC surface added.

## Validation: FAIL (2026-08-05T14:57:33.900Z)

pytest: 123 passed, ruff: clean — the code works locally. However, three files are not committed: (1) tools/sim/src/sim/run_round3.py is untracked (never git-added), (2) tools/sim/pyproject.toml has an unstaged entry-point addition (line 18: sim-round3-sweep), and (3) tools/sim/tests/test_run_round3.py has an unstaged tolerance fix (line 140: delta threshold changed from 0.15 to 0.30, docstring updated to explain k_c's indirect effect via pickup dilution). The committed tree contains only the red-state test commit (1762cce) which would fail because sim.run_round3 does not exist. All three files must be staged and committed (with Co-authored-by trailer) for the card to be complete. Commit trailer on existing commit: present.

## Validation: FAIL (2026-08-05T15:38:29.631Z)

python-verify step could not be executed: `pip install -e '.[dev]'` in tools/sim was denied by the permission system (tried multiple path forms — cd-chained, relative, and absolute). Without pip install, pytest and ruff cannot run against the installed package, so the test suite is unverified. The diff itself looks correct on code-review inspection (TDD order confirmed: test commit 1762cce before implementation 4e9d8ee, Co-authored-by trailer present on both, no free-text UGC surface, working tree clean, all three changed files committed), but per fail-closed policy a check that could not run is a FAIL. To unblock: grant the reviewer Bash permission for pip install within the tools/sim venv (e.g. allow pattern `*/tools/sim/.venv/bin/pip*`).

## Validation: FAIL (2026-08-05T21:12:14.743Z)

pytest: 123 passed — all tests green. ruff: 1 error — tests/test_run_round3.py:143 E501 line too long (114 > 100). The assert message string on that line exceeds the 100-char limit configured in pyproject.toml. Fix: break the f-string or shorten it. TDD order confirmed (test commit 1762cce before implementation 4e9d8ee), Co-authored-by trailer present on both commits, working tree clean, no free-text UGC surface added, all three changed files committed. The only blocker is the ruff E501 violation.

## Validation: PASS (2026-08-05T22:01:34.637Z)

python-verify: pytest 123 passed, ruff clean. TDD order confirmed (test commit 1762cce before implementation 4e9d8ee). Co-authored-by trailer present on all 4 commits. Working tree clean — all three changed files (pyproject.toml entry-point, src/sim/run_round3.py, tests/test_run_round3.py) committed. No free-text UGC surface added. Code correctly sweeps k_c, k_r, unique_count jointly with population and derives the floor as a surface via population_floor_surface(), satisfying all three acceptance criteria. Previous FAIL items resolved: tolerance adjusted to 0.30 with docstring explaining indirect k_c effect, ruff E501 fixed in commits 5611f43 and cbd249a, all files staged and committed.

## PR (2026-08-05T22:01:34.637Z)

https://github.com/dennie-seth/assembled/pull/72
