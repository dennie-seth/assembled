---
id: "T-0024"
title: "Runner lifecycle state machine"
status: "backlog"
priority: "P1"
phase: 2
agent: "infra"
depends_on: ["T-0020", "T-0022"]
created: "2026-08-01"
---

## Context

Card status <-> git state mapping from `docs/PLAN.md` Section 2: `ready`
-> agent starts -> `in-progress` -> agent reports done -> `review`. On
failure, the card moves to `blocked` with a reason attached. **`review` is
terminal for automation** - no code path may move a card to `done`; that's
a human-only gate (root `CLAUDE.md`, non-negotiable).

**Bootstrap note:** implemented directly this session.

## Acceptance

- [ ] State machine covers ready->in-progress->review and
      in-progress->blocked(reason) transitions
- [ ] Test: illegal transitions (e.g. review->in-progress via automation)
      are rejected
- [ ] No code path in this task can set a card to `done`
