---
id: "T-0136"
title: "Fetch T-0072 corpus images and attach them to the ticket"
status: "done"
priority: "P2"
phase: 6
agent: "assets"
depends_on: ["T-0072"]
created: "2026-08-05"
branch: "feature/T-0136"
commit: "f65602f3cf377fd04a96cfe08b3ddbc17a892a0d"
pr: "https://github.com/dennie-seth/assembled/pull/73"
deliverable_type: "artifact"
comments: []
attachments: []
---
As the LoRA pipeline, I want the T-0072 reference/corpus images fetched and attached to the T-0072 ticket so the training corpus is visible and downloadable directly from the board.

The images are fetched by running the `lora_train` fetch tooling merged in PR #65 (e.g. `python -m lora_train.fetch --download`), then each fetched image is uploaded to T-0072 via the board's attachments endpoint: `POST /api/tasks/T-0072/attachments` with multipart `file=@<path>;type=image/png` and `uploaded_by=<agent>` (one request per file).

## Acceptance

- [ ] T-0072's Attachments section shows the fetched corpus images

## Validation: FAIL (2026-08-05T19:49:28.530Z)

pytest passes (58/58), but ruff check reports 8 errors that must be fixed before the card can pass. In attach.py: I001 (unsorted imports, line 13), UP012 (unnecessary utf-8 encoding arg, lines 43, 50, 62). In test_attach.py: I001 (unsorted imports, line 10), F401 (unused imports: io at line 12, pathlib at line 13, unittest.mock.call at line 16). Per .claude/rules/python.md: 'Run ruff check . before committing; CI enforces it.' All 8 are auto-fixable with ruff --fix.

## Validation: FAIL (2026-08-05T21:17:20.192Z)

pytest 58/58 passed. ruff check still reports 2 errors: I001 (unsorted/unformatted import block) at src/lora_train/attach.py:13 and tests/test_attach.py:10. The fix commit 980f241 resolved the UP012 and F401 errors from the prior review but did not fix the I001 import-sorting violations. Per .claude/rules/python.md: 'Run ruff check . before committing; CI enforces it.' Both are auto-fixable with ruff check --fix. TDD order is correct (test commit 4766c5d before impl 3703ff5), commit trailers present on all three T-0136 commits, no free-text UGC surface, code is otherwise clean.

## Validation: PASS (2026-08-05T22:14:43.972Z)

Python verify executed in full: pytest 58/58 passed, ruff check reports 'All checks passed!' — both I001 import-sorting violations from the prior two FAIL reviews are resolved by commit f65602f. TDD order confirmed: test commit 4766c5d (red) before implementation 3703ff5 (green). All 4 T-0136 commits carry the required Co-authored-by trailer. No free-text UGC surface introduced. Code is clean, focused, and well-tested with proper mocking of urllib.request.urlopen.

## PR (2026-08-05T22:14:43.972Z)

https://github.com/dennie-seth/assembled/pull/73
