---
id: "T-0044"
title: "NoteRepo interface + PG implementation"
status: "backlog"
priority: "P1"
phase: 4
agent: "server"
depends_on: ["T-0042", "T-0043"]
created: "2026-08-01"
---

## Context

**Revised per `docs/HANDOFF.md` Section 3.** Originally scoped with a
radius query. **The radius query is deleted.** Lookup is now equality on
`(archetype_id, anchor_tag)` (D-1, discrete world, no geometry) - this repo
is simpler than originally planned, not just differently shaped.

## Acceptance

- [ ] `NoteRepo` interface + Postgres implementation: create, fetch by
      `(archetype_id, anchor_tag)`, rate
- [ ] Test: CRUD round-trip
- [ ] Test: FK violation (bad `template_id`/`slot_*`) is rejected, not
      silently accepted
- [ ] No radius/coordinate parameter exists anywhere in this repo's
      interface
