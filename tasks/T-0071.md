---
id: "T-0071"
title: "AssetAgent: workflow JSON -> /prompt -> poll /history -> fetch /view"
status: "backlog"
priority: "P1"
phase: 6
agent: "assets"
depends_on: ["T-0070"]
created: "2026-08-01"
---

## Context

The agent that drives ComfyUI: takes a workflow JSON template, submits it
via `POST /prompt`, polls `/history` for completion, and fetches the
result via `/view`. Base class shared with the audio pipeline's
`AudioAgent` (T-0082) - DRY per `docs/PLAN.md` Phase 7.

## Acceptance

- [ ] Submits a workflow, polls to completion, retrieves output
      end-to-end against the local ComfyUI instance (T-0070)
- [ ] **Guardrail:** refuses to run a workflow whose checkpoint isn't on
      the approved-license allowlist (Apache-2.0/OpenRAIL/CC0-derived) -
      encoded as an enforced check, not just a convention
