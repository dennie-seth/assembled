---
id: "T-0141"
title: "Scaling bug"
status: "done"
priority: "P0"
phase: 0
agent: "infra"
depends_on: []
created: "2026-08-06"
branch: "feature/T-0141"
commit: "6aba9376de4a22b78aa4e14d996471336ce9d989"
pr: "https://github.com/dennie-seth/assembled/pull/87"
deliverable_type: "code"
attempts: 0
comments: []
attachments: []
---
When side-menu appears, text in cards doesn't scale properly and overlaps neighbour cards. As a user I want side-menu to cover cards and scroll right and left to see them properly once necessary.

## Validation: FAIL (2026-08-06T14:53:46.121Z)

Tests pass (63 files, 1121 tests green), lint clean, TDD order correct (test commit 564a5d1 before fix commit 11d9f9b), commit trailers present, no conduct violations. However, tasks/T-0141.md has no ## Acceptance or ## Context sections — only a bare description. Per reviewer instructions, a card without a parseable Acceptance checklist cannot be verified and is itself a FAIL. The card needs ## Context and ## Acceptance sections with checkable criteria (e.g. 'columns do not shrink when side-panel is visible', 'board scrolls horizontally to reveal covered columns', 'body no longer adds padding-right for side-panel') before this can pass review.

(run 1 of 5)

## Validation: FAIL (2026-08-06T14:57:11.689Z)

All three required verification commands were denied by the Bash permission system: (1) `node tools/board/scripts/validateBacklog.js` — backlog validator, (2) `node tools/board/scripts/checkPlannerDiffGuard.js develop` — planner diff guard, (3) `npm test` from tools/board — board test/lint suite. Per reviewer instructions, a required check that could not be run is a FAIL, not an unverified pass. The diff itself looks correct on read-only inspection: .column flex:1 0 14rem (style.css:71), .board overflow-x:auto (style.css:66), #side-panel position:fixed (style.css:130), body padding-right rule removed, TDD order correct (test 564a5d1 before fix 11d9f9b), all commit trailers present, card now has Context and Acceptance sections. Grant the reviewer Bash permission for `node tools/board/scripts/validateBacklog.js`, `node tools/board/scripts/checkPlannerDiffGuard.js`, and `npm test --prefix tools/board` (or equivalent) to unblock this validation.

(run 2 of 5)

## Validation: FAIL (2026-08-06T14:59:29.799Z)

All three required verification commands were denied by Bash permissions (run 3 of 5): (1) `node tools/board/scripts/validateBacklog.js` — backlog validator, (2) `node tools/board/scripts/checkPlannerDiffGuard.js develop` — planner diff guard, (3) `npm test --prefix tools/board` — board test/lint suite. Without these, acceptance criteria cannot be machine-verified. On read-only diff inspection the changes look correct: style.css removes the `body:has(#side-panel:not([hidden]))` padding-right rule, changes `.column` from `flex: 1 1 0; min-width: 12rem` to `flex: 1 0 14rem`, `#side-panel` is `position: fixed`, `.board` has `overflow-x: auto`; TDD order is correct (test commit 564a5d1 before fix 11d9f9b); all commits carry the `Co-authored-by: Claude <noreply@anthropic.com>` trailer; the card now has well-formed ## Context and ## Acceptance sections. To unblock this validation, grant the reviewer Bash permission for `node tools/board/scripts/validateBacklog.js`, `node tools/board/scripts/checkPlannerDiffGuard.js develop`, and `npm test --prefix tools/board` (or equivalent).

(run 3 of 5)

## Validation: FAIL (2026-08-06T15:02:35.970Z)

Backlog validator and planner diff guard both passed. Read-only diff inspection confirms all six acceptance criteria appear met in the source: .column flex:1 0 14rem (style.css:71), .board overflow-x:auto (style.css:66), #side-panel position:fixed (style.css:130), body padding-right rule removed, test file has 4 matching assertions, TDD order correct (test 564a5d1 before fix 11d9f9b), all commit trailers present, no conduct or js.md violations. However, the required board test/lint suite could not be run — all attempts to execute `npm test --prefix tools/board`, `cd tools/board && npm test`, `npx --prefix tools/board vitest run`, and `node tools/board/node_modules/.bin/vitest run` were denied by the Bash permission system. Per reviewer instructions a required check that could not be run is a FAIL. To unblock: grant the reviewer Bash permission for `npm test` (or `vitest run`) scoped to `tools/board`.

(run 4 of 5)

## Blocked (2026-08-06T15:02:40.600Z)

implementer process exited with code 1

## Validation: FAIL (2026-08-06T18:47:48.146Z)

Backlog validator and planner diff guard both passed. Read-only diff inspection confirms all six acceptance criteria appear met in CSS source: .column flex:1 0 14rem (style.css:71), .board overflow-x:auto (style.css:66), #side-panel position:fixed (style.css:130), body padding-right rule removed, test file has 4 matching assertions (columnLayout.test.js), TDD order correct (test 564a5d1 before fix 11d9f9b), all commit trailers present, no conduct or js.md violations. However, the required board test/lint suite could not be run — all attempts to execute npm test, vitest run (via node_modules/.bin), and npx vitest were denied by the Bash permission system. Per reviewer instructions a required check that could not be run is a FAIL. To unblock: grant the reviewer Bash permission for npm test (or vitest run) scoped to tools/board.

(run 1 of 5)

## Validation: PASS (2026-08-06T18:54:44.275Z)

All three required verification commands executed and passed: backlog validator (97 tasks), planner diff guard (no violations), board test suite (63 files, 1121 tests green), ESLint clean. All six acceptance criteria verified against source: (1) .column flex-shrink:0 at style.css:71, (2) flex-basis 14rem at style.css:71, (3) #side-panel position:fixed at style.css:130, (4) body:has(#side-panel:not([hidden])) padding-right rule confirmed removed (present at merge base 0bcedc7 lines 17-19, absent at HEAD), (5) .board overflow-x:auto at style.css:66, (6) all 4 columnLayout.test.js assertions pass. TDD order correct (test commit 564a5d1 before fix 11d9f9b), all commits carry Co-authored-by trailer, no conduct or js.md violations.

## PR (2026-08-06T18:54:44.275Z)

https://github.com/dennie-seth/assembled/pull/87
