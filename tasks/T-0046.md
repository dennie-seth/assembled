---
id: "T-0046"
title: "GET /v1/notes: tag equality + ranking"
status: "backlog"
priority: "P1"
phase: 4
agent: "server"
depends_on: ["T-0044"]
created: "2026-08-01"
---

## Context

**Revised per `docs/HANDOFF.md` Section 3.** Originally "radius+ranking" -
now **tag equality + ranking**, much smaller in scope. **Drop the GiST
index entirely** - `GET /v1/notes?archetype_id&anchor_tag&limit` orders by
`score DESC` off the plain btree index from `04-data-model.md` Section 5.
This was PLAN.md open question 2, now closed (D-1).

## Acceptance

- [ ] `GET /v1/notes?archetype_id&anchor_tag&limit` returns notes ordered
      by `score DESC`
- [ ] Test: ordering correctness
- [ ] Test: `limit` clamped to a sane maximum server-side
- [ ] Test: empty result for an anchor with no notes (not an error)
- [ ] No GiST index, no radius/coordinate parameter anywhere
