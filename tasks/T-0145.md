---
id: "T-0145"
title: "Flow health: rework rate 65% over 65 validations (auto-proposed 2026-08-07)"
status: "retired"
priority: "P2"
phase: 0
agent: null
depends_on: []
created: "2026-08-07"
branch: null
commit: null
pr: null
deliverable_type: "code"
attempts: 5
comments: []
attachments: []
---
<!-- flow-stats-self-improve: baseline-done=57 -->

## Context

Auto-proposed by the flow-stats self-improvement loop (see `docs/design/flow-stats-self-improvement.md`).

Snapshot: 99 total cards.
- backlog: 40
- ready: 0
- in-progress: 0
- validation: 0
- review: 0
- done: 57
- blocked: 0
- retired: 2

Rework rate: 65% (42 FAIL / 65 validation notes).
Cards blocked after exhausting the auto-retry cap: 0.
Orphan-reaper recoveries recorded: 1.
Average rework per card that reached done: 0.65.

Triggered: rework rate 65% over the last 65 validation notes (threshold crossed).

## Acceptance

- [ ] Root cause identified for the flow signal above (cite specific card ids and FAIL/Blocked note text, not just the aggregate numbers)
- [ ] At least one concrete infra fix implemented (grant fix, prompt fix, guard fix, or similar) addressing the identified cause
- [ ] A following flow-stats snapshot shows the number moved, or this card documents why improvement isn't yet measurable

## Validation: FAIL (2026-08-07T11:38:32.652Z)

Three failures. (1) The implementation is entirely missing: the card prescribes three fixes — adding `Bash(npm test:*)` to `.claude/agents/reviewer.md`, adding a lint auto-fix step to `.claude/skills/tdd/SKILL.md`, and adding a card-structure check to `tdd/SKILL.md` Step 1 — but neither file was modified in the diff. The implementer wrote the failing tests (red) but never wrote the code to make them pass (green). All three new tests fail: `toolAllowlist.test.js:259` (reviewer.md missing `Bash(npm test:*)`), `skillContent.test.js:18` (SKILL.md missing Context/Acceptance add instruction), `skillContent.test.js:30` (SKILL.md missing `ruff check --fix`/`eslint --fix`). Acceptance criteria 1–4 are all unmet. (2) Planner diff guard failed: `tasks/T-0145.md: status changed from 'validation' to 'ready' on T-0145 -- planner runs must never touch status`. The card's status field was modified in the diff. (3) Acceptance criterion 5 (root-cause analysis in card body) is met — the Context section contains a thorough analysis with specific card IDs (T-0129, T-0131, T-0132, T-0136, T-0043, T-0141, T-0137, T-0139, T-0140, T-0143) and FAIL note text. But 4 of 5 acceptance criteria are unmet, so the card cannot pass.

(run 1 of 5)

## Validation: FAIL (2026-08-07T11:40:58.805Z)

Three failures. (1) Planner diff guard: `tasks/T-0145.md: status changed from "validation" to "ready"` — the card's status field was modified in the diff, violating the diff guard. (2) Implementation entirely missing: the card prescribes three fixes — adding `Bash(npm test:*)` to `.claude/agents/reviewer.md` tools, adding a lint auto-fix step to `.claude/skills/tdd/SKILL.md`, and adding a card-structure check to `tdd/SKILL.md` Step 1 — but neither file was modified in the diff. The implementer wrote the failing tests (red) but never wrote the code to make them pass (green). All three new tests fail: `toolAllowlist.test.js:259` (reviewer.md missing `Bash(npm test:*)`), `skillContent.test.js:18` (SKILL.md missing Context/Acceptance add instruction), `skillContent.test.js:30` (SKILL.md missing `ruff check --fix`/`eslint --fix`). Card acceptance criteria 1–4 are all unmet. (3) Only acceptance criterion 5 (root-cause analysis in card body) is met — the Context section contains a thorough analysis with specific card IDs. But 4 of 5 card-level acceptance criteria are unmet, and 2 of 3 prompt-level acceptance criteria are unmet, so the card cannot pass. The implementer must: (a) revert the status change in `tasks/T-0145.md` back to whatever it was on develop, (b) edit `.claude/agents/reviewer.md` to add `Bash(npm test:*)` to the tools line, (c) edit `.claude/skills/tdd/SKILL.md` to add the lint auto-fix instruction and the card-structure check to Step 1, and (d) confirm all three new tests pass.

(run 2 of 5)

## Validation: FAIL (2026-08-07T11:58:08.838Z)

Planner diff guard failed: `tasks/T-0145.md: status changed from "validation" to "ready" on T-0145 -- planner runs must never touch status`. The card's status field was modified in the diff (from `validation` on develop to `ready` on the branch). This is the same violation reported in runs 1 and 2. The three implementation fixes are all correctly in place (reviewer.md has `Bash(npm test:*)`, tdd SKILL.md Step 1 has the Context/Acceptance check, tdd SKILL.md Step 4 has the lint auto-fix instruction), all 906 board tests pass, ESLint is clean, backlog validator passes, and all three prompt-level acceptance criteria are met — but the diff guard violation is an automatic FAIL. The implementer must revert `tasks/T-0145.md`'s `status` field back to `validation` (matching develop) and its `attempts` field back to `3` (matching develop) before the card can pass. The `agent` field change from `null` to `"infra"` is also a diff-guard-visible modification that should be reverted to match develop.

(run 3 of 5)

## Validation: FAIL (2026-08-07T12:01:49.393Z)

Planner diff guard FAILED: `tasks/T-0145.md: status changed from "validation" to "in-progress" on T-0145 -- planner runs must never touch status`. On develop the card has `status: "validation"`; on this branch it has `status: "in-progress"`. This is the same class of violation that caused runs 1–3 to fail. The implementer must revert `tasks/T-0145.md`'s frontmatter `status` field back to `"validation"` (matching develop exactly). All three implementation fixes are correctly in place (reviewer.md has `Bash(npm test:*)`, tdd SKILL.md Step 1 has the Context/Acceptance add instruction, tdd SKILL.md Step 4 has the lint auto-fix instruction), all 906 tests pass, ESLint is clean, backlog validator passes, and all three prompt-level acceptance criteria are met — but the diff guard violation is an automatic FAIL. Fix: change line 4 of `tasks/T-0145.md` from `status: "in-progress"` back to `status: "validation"`.

(run 4 of 5)

## Validation: FAIL (2026-08-07T12:06:24.132Z)

Planner diff guard FAILED: tasks/T-0145.md status changed from 'validation' (on develop) to 'in-progress' (on branch) — same violation as runs 1–4. The 'attempts' field also changed (develop has a different value). All three implementation fixes are correctly in place (reviewer.md line 4 has Bash(npm test:*), tdd SKILL.md Step 1 has Context/Acceptance add instruction, tdd SKILL.md Step 4 has ruff check --fix / eslint --fix instruction), 906 tests pass, ESLint clean, backlog validator passes, TDD ordering confirmed (test commit 8ea45a5 before impl 8d56d09), all commit trailers present, and all 3 prompt-level acceptance criteria are met. The ONLY remaining blocker is the diff guard: the implementer must revert tasks/T-0145.md frontmatter to match develop exactly (status: 'validation', and the attempts/agent fields as they appear on develop). The card body changes (root-cause analysis, expanded acceptance) are fine — only the frontmatter fields that the diff guard checks (status) must match.

(run 5 of 5) Auto-retry limit reached -- blocked for human review.
