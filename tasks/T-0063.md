---
id: "T-0063"
title: "NoteClient GDExtension node: post/fetch/rate"
status: "backlog"
priority: "P1"
phase: 5
agent: "client"
depends_on: ["T-0061", "T-0062", "T-0045", "T-0046"]
created: "2026-08-01"
---

## Context

GDExtension node wrapping the notes API from
`docs/design/03-net-protocol.md` Section 2: post, fetch (by
`archetype_id`/`anchor_tag`, no radius), rate. **Does not include `roll`**
- `/v1/roll` is superseded (see T-0048); there is no client-side roll call
to wire up.

## Acceptance

- [ ] `NoteClient` node exposes post/fetch/rate against a mock server
- [ ] Test: timeout handling
- [ ] Test: 4xx/5xx response paths surface as distinguishable client-side
      states, not a generic failure
