---
id: "T-0023"
title: "Prompt builder: task md + rules -> prompt"
status: "backlog"
priority: "P1"
phase: 2
agent: "infra"
depends_on: []
created: "2026-08-01"
---

## Context

Builds the actual prompt text handed to `claude -p` from a task card's
markdown body plus any `.claude/rules/*.md` whose `paths:` frontmatter
matches files the task is likely to touch. Independent of the runner
process itself - pure string construction from inputs already on disk.

**Bootstrap note:** implemented directly this session.

## Acceptance

- [ ] Test: template correctness for a representative task card
- [ ] Test: task body is injected verbatim, not summarized or truncated
- [ ] Test: only rules whose `paths:` glob matches the task's declared
      scope are included
