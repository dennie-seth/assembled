---
id: "T-0050"
title: "Structured logging + /healthz"
status: "backlog"
priority: "P2"
phase: 4
agent: "server"
depends_on: ["T-0040"]
created: "2026-08-01"
---

## Context

Structured (JSON) logging across server handlers, plus a `/healthz`
endpoint for liveness checks (CI, deployment, monitoring).

## Acceptance

- [ ] Handlers log structured entries (request id, route, status, timing)
- [ ] `GET /healthz` returns 200 when the server and DB connection are
      healthy, non-200 otherwise
