---
id: "T-0026"
title: "Persist run log to tasks/.runs/"
status: "backlog"
priority: "P2"
phase: 2
agent: "infra"
depends_on: ["T-0021"]
created: "2026-08-01"
---

## Context

Every run's parsed NDJSON stream (T-0021) is persisted to
`tasks/.runs/T-NNNN-<ts>.jsonl` so a run's output survives past the
session that produced it and can be replayed in the card detail view.

**Bootstrap note:** implemented directly this session.

## Acceptance

- [ ] Every run writes a `tasks/.runs/T-NNNN-<ts>.jsonl` file
- [ ] File is append-only during the run, valid NDJSON when complete
- [ ] Card detail view can load and replay a past run's log
