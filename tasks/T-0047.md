---
id: "T-0047"
title: "Note rating, one vote per player"
status: "backlog"
priority: "P2"
phase: 4
agent: "server"
depends_on: ["T-0046"]
created: "2026-08-01"
---

## Context

`POST /v1/notes/{id}/rate {val: +1|-1}`. One vote per `(note_id, voter)`,
idempotent - resubmitting the same value is a no-op, changing it overwrites
the prior vote. This is the base rating mechanism; T-0098 layers the
proof-of-play gate on top of this endpoint.

## Acceptance

- [ ] Test: double-vote with the same value is idempotent (no state change,
      no error)
- [ ] Test: changing an existing vote updates it rather than adding a
      second row
- [ ] Note `score` reflects the current rating tally correctly
