---
id: "T-0137"
title: "ensure tools/board/node_modules is pre-installed in the worktree"
status: "done"
priority: "P0"
phase: 0
agent: "infra"
depends_on: []
created: "2026-08-05"
branch: "feature/T-0137"
commit: "9a6362d4fa48abf180532033b2528b42a6bf3074"
pr: "https://github.com/dennie-seth/assembled/pull/80"
deliverable_type: "code"
attempts: 0
comments: [{"author":"Anonymous","text":"carefully resolve conflicts with develop","timestamp":"2026-08-06T14:39:03.544Z"}]
attachments: []
---
As a user I want to ensure tools/board/node_modules is pre-installed in the worktree so verification agent wouldn't have to run npm install as T-0043 validator suggests.

## Context

The reviewer for T-0043 (and others) noted that fresh git worktrees created
by the Agent Runner lack a `tools/board/node_modules` directory, forcing the
verification agent to run `npm install` before it can execute tests. This
adds friction and risks version drift. The fix is to symlink
`tools/board/node_modules` from the canonical main-worktree installation into
each new worktree at worktree-creation time (`docs/PLAN.md` §Phase 0 →
runner/orchestrator task lifecycle).

## Acceptance

- [ ] `linkBoardNodeModules({ worktreeDir, repoRoot })` is exported from
      `tools/board/src/runner/gitOps.js` and creates a symlink at
      `<worktreeDir>/tools/board/node_modules` pointing to
      `<repoRoot>/tools/board/node_modules`
- [ ] The function is best-effort and idempotent: if the source does not yet
      exist it returns without error; if the destination already exists it
      returns without error
- [ ] `createWorktree` in `gitOps.js` calls `linkBoardNodeModules`
      automatically after creating a worktree
- [ ] `.gitignore` (at repo root or `tools/board/`) excludes the symlink so
      it is never committed
- [ ] Vitest tests for `linkBoardNodeModules` pass (covering: symlink
      created when source exists, silently skipped when source missing,
      idempotent when destination already exists)
- [ ] All existing tests continue to pass (945/945 or equivalent)

## Recovered (2026-08-05T23:19:13.887Z)

run did not complete (board restarted or process ended before a verdict); reset to blocked for re-run.

## Validation: FAIL (2026-08-06T12:38:33.395Z)

Card T-0137 has no `## Acceptance` section (and no `## Context` section either). Per reviewer rules, a card without checkable acceptance criteria cannot be verified — that is itself a FAIL regardless of code quality. The code itself is sound: 945/945 tests pass, eslint clean, TDD commit order correct (tests in 9d6a455 before implementation in 76b8d27), all commits carry the Co-authored-by trailer, .gitignore change correctly drops trailing slash to cover symlinks, linkBoardNodeModules() is best-effort and idempotent as documented. The card needs a proper `## Context` and `## Acceptance` section added before it can pass review.

(run 1 of 5)

## Validation: PASS (2026-08-06T12:43:16.684Z)

All required checks executed and passed: 945/945 vitest tests green, eslint clean, backlog validator passed (93 tasks), planner diff guard passed (no status/deletion violations). Acceptance criteria verified: (1) linkBoardNodeModules exported from gitOps.js, creates symlink from repoRoot to worktreeDir node_modules — confirmed in diff; (2) best-effort (returns silently if source missing) and idempotent (returns if dest exists) — confirmed in code and tested; (3) called automatically after worktree creation — the call is in runOrchestrator.runCard after addWorktree (the AC names 'createWorktree' which doesn't exist, but the intent is satisfied and tested in runOrchestrator.test.js); (4) .gitignore updated from 'node_modules/' to 'node_modules' to cover symlinks; (5) three vitest tests cover symlink creation, idempotency, and source-missing scenarios; (6) all 945 existing tests pass. TDD commit order correct: tests in 9d6a455 before implementation in 76b8d27. All commits carry Co-authored-by trailer. No free-text UGC surface added. No conduct violations.

## PR (2026-08-06T12:43:16.684Z)

https://github.com/dennie-seth/assembled/pull/80

## Validation: PASS (2026-08-06T14:45:34.948Z)

All required checks executed and passed. Backlog validator: 94 tasks checked, passed. Planner diff guard: 1 tasks/ file changed, no status/deletion violations. Vitest: 1121/1121 tests pass (62 test files). ESLint: clean, no errors. Acceptance criteria verified individually: (1) linkBoardNodeModules exported from gitOps.js, creates symlink from repoRoot to worktreeDir node_modules — confirmed in diff at gitOps.js:272-290; (2) best-effort (returns silently if source missing via fs.stat catch) and idempotent (returns if dest exists via fs.lstat check) — confirmed in code and tested; (3) called automatically after worktree creation — runOrchestrator.js calls this.git.linkBoardNodeModules after addWorktree (AC names 'createWorktree' which doesn't exist as a function, but the intent is satisfied and tested in runOrchestrator.test.js); (4) .gitignore updated from 'node_modules/' to 'node_modules' to cover symlinks — confirmed in diff; (5) three vitest tests cover symlink creation, idempotency, and source-missing scenarios — confirmed in gitOps.test.js; (6) all 1121 existing tests pass. TDD commit order correct: tests in 9d6a455 before implementation in 76b8d27. All implementation commits carry Co-authored-by trailer. No free-text UGC surface added. ESM imports used throughout. No conduct violations.

## PR (2026-08-06T14:45:34.948Z)

https://github.com/dennie-seth/assembled/pull/80
