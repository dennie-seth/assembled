---
id: "T-0147"
title: "Change flow health cadence to 1 per week"
status: "in-progress"
priority: "P0"
phase: 0
agent: "infra"
depends_on: []
created: "2026-08-07"
branch: null
commit: null
pr: null
deliverable_type: "code"
attempts: 5
comments: []
attachments: []
---
As a user I want automatic Flow health check task to run once per week not to spam board with same tasks.

## Validation: FAIL (2026-08-07T12:32:54.020Z)

Card T-0147 has no ## Acceptance section — its body is a single user-story sentence with no checkable criteria. Per reviewer conventions, a card without parseable acceptance criteria cannot be verified and is itself a FAIL. All mechanical checks passed: 67 test files / 1180 tests green, ESLint clean, backlog validator and planner diff guard both pass, TDD ordering correct (test commit 6bc3217 before impl bb9c324), Co-authored-by trailers present on both substantive commits. The implementation itself (replacing card-count interval with 7-day time-based interval in selfImprovementTrigger.js and flowImprovementCard.js) appears correct and well-tested. To unblock: add a ## Context and ## Acceptance section to tasks/T-0147.md with specific, checkable criteria (e.g. 'evaluateTrigger fires when >= 7 days have elapsed since the last auto-proposed card', 'env var FLOW_STATS_SELFIMPROVE_INTERVAL_DAYS controls the interval', 'first run with no prior card fires immediately').

(run 1 of 5)

## Validation: FAIL (2026-08-07T12:36:30.967Z)

All mechanical checks passed except the planner diff guard: `node tools/board/scripts/checkPlannerDiffGuard.js develop` exited 1 with 'tasks/T-0147.md: status changed from "validation" to "ready" on T-0147 -- planner runs must never touch status'. The status change is from the orchestrator's lifecycle (post-FAIL verdict reset), not an implementer edit, but the diff guard is a mechanical gate and its failure is an automatic FAIL per reviewer conventions. Tests: 67 files / 1180 tests green. ESLint: clean. Backlog validator: 103 tasks passed. TDD ordering correct (test commit 6bc3217 before impl bb9c324). Co-authored-by trailers present on substantive commits. Card now has proper ## Context and ## Acceptance sections with 5 specific criteria. The implementation itself (time-based weekly interval replacing card-count interval) appears correct and well-tested. To unblock: the diff guard compares against develop where T-0147's status is 'validation'; the branch has it as 'ready'. Either rebase/reset the card status on the branch to match develop, or have the orchestrator ensure the base branch's card status matches HEAD before invoking the reviewer.

(run 2 of 5)

## Validation: FAIL (2026-08-07T12:40:40.258Z)

Planner diff guard FAILED (exit 1): 'tasks/T-0147.md: status changed from "validation" to "in-progress" on T-0147 -- planner runs must never touch status'. On develop T-0147 is at status 'validation' (set by the orchestrator after run 2), but commit 62420aa on this branch set it to 'in-progress' to match develop's earlier state -- develop has since moved. This is the same root cause as run 2: the branch's card status drifts from develop between reviewer runs. Additionally, 1/1180 tests failed in httpApi.commitOnUpdate.test.js:188 (a pre-existing flaky git-temp-repo test unrelated to this diff, but noted for completeness). All other checks passed: ESLint clean, backlog validator 103 tasks OK, TDD ordering correct (test 6bc3217 before impl bb9c324), Co-authored-by trailers present, card now has proper Context+Acceptance sections. To unblock: the orchestrator should update the branch's T-0147.md status to match develop's current value immediately before each reviewer invocation, not in a commit that can go stale.

(run 3 of 5)

## Validation: FAIL (2026-08-07T12:45:57.990Z)

Planner diff guard FAILED (exit 1): 'tasks/T-0147.md: status changed from "validation" to "in-progress" on T-0147 -- planner runs must never touch status'. On develop T-0147 is at status 'validation' (set by the orchestrator after run 3's FAIL verdict), but commit 62420aa on this branch set it to 'in-progress' — the same status-drift root cause as runs 2 and 3. All other checks passed: Vitest 67 files / 1180 tests green, ESLint clean, backlog validator 103 tasks OK, TDD ordering correct (test commit 6bc3217 before impl bb9c324), Co-authored-by trailers present on both substantive commits. Card now has proper Context + Acceptance sections; all 5 acceptance criteria are satisfied by the implementation (time-based weekly interval in selfImprovementTrigger.js, env var FLOW_STATS_SELFIMPROVE_INTERVAL_DAYS, first-run immediate fire, open-card suppression). The implementation is correct and complete — the only blocker is the diff guard's status mismatch, which is an orchestrator-level issue: the orchestrator must update the branch's tasks/T-0147.md status to match develop's current value immediately before each reviewer invocation, not in a commit that can go stale between runs.

(run 4 of 5)
