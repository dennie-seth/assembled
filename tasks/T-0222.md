---
id: "T-0222"
title: "Remediate 9 T-0214 entity-v2 provenance records for P-7 (drop non-resolvable annotation)"
status: "blocked"
priority: "P1"
phase: 6
agent: "assets"
depends_on: []
created: "2026-08-23"
branch: "feature/T-0222"
commit: null
pr: null
deliverable_type: "code"
requires_approval: false
approved_by: null
approved_at: null
attempts: 2
comments: []
attachments: []
---

## Context

Option A (remediate, don't grandfather) on the [P-7 gate](T-0219) (PR #234): the gate correctly flags 14 pre-existing non-compliant assets. 5 are the signal_tower props (covered by [T-0221](T-0221)). The other 9 are the T-0214 entity-v2 sheets — sound / still_air / watcher × idle / move / trapped — whose `provenance.generator` reads `"assets/src/character/gen_entities_v2.js (curl-only pipeline, T-0214)"`. The free-text suffix `" (curl-only pipeline, T-0214)"` breaks P-7's resolver even though the leading path is real.

**Verified truth (checked directly against `origin/develop` before filing this card):** `assets/src/character/gen_entities_v2.js` **does exist**, committed on `origin/develop` (875 lines, a genuine img2img+LoRA pipeline: crop concept section -> tile -> ComfyUI img2img+LoRA -> descend -> stitch -> write indexed PNG + provenance). This is a real generator, not a stub or an ad-hoc uncommitted script — this is NOT the same failure mode as the signal_tower props ([T-0221](T-0221)), where the named generator (`SolidMask + JoinImageWithAlpha`) genuinely does not exist anywhere in the repo. Do not re-verify this from scratch; confirm it still holds at execution time (repo state may have moved) and proceed on the "exists" branch below unless that's no longer true, in which case escalate rather than silently switching remediation strategy.

## Story

Since the generator exists, the fix is a provenance correction, not a regeneration:

1. For each of the 9 sidecars under `assets/final/entity/{sound,still_air,watcher}_{idle,move,trapped}_sheet_v2.provenance.json`, change `generator` from `"assets/src/character/gen_entities_v2.js (curl-only pipeline, T-0214)"` to the bare resolvable path `"assets/src/character/gen_entities_v2.js"`. Do not touch any other field (model_hash, seed, prompt, etc. all stay as-is — this is a provenance-accuracy fix, not a regeneration).
2. Confirm the referenced generator genuinely produces these sheets: read through `gen_entities_v2.js` and cross-check its documented pipeline (concept-conditioned img2img via `entities_concept_sheet_v1.png`, SDXL + `soviet_brutalism_style_v1.safetensors` LoRA, per-cell descent) against the 9 sidecars' recorded params (model_hash, lora_hash, concept_hash, seed, steps, cfg, sampler, denoise) — they should already match since T-0214 ran this exact script; the point is to confirm this by inspection/test, not assume it. If `tools/asset-gate` has (or [T-0219](T-0219) adds) a way to statically verify a `generator` path resolves and is non-trivial (not empty/stub), run it against these 9 as part of this card's verification.
3. Run the full asset gate against these 9 assets: [T-0219](T-0219)'s P-7 resolvability sweep must now pass on all 9, and the existing `visibility-sweep` must also pass (confirm the v2 PNGs render real content, not blank — they're real SDXL output per T-0214 so this is expected to already hold, verify rather than assume).

## Acceptance

- [x] Confirmed `assets/src/character/gen_entities_v2.js` exists and is a real, non-trivial committed generator (document this confirmation in the PR description/commit message, including how it was checked)
- [x] All 9 `assets/final/entity/*_v2.provenance.json` sidecars corrected: `generator` field is the bare path `assets/src/character/gen_entities_v2.js` with no free-text annotation suffix
- [x] No other provenance fields changed (model_hash, seed, prompt, concept_hash, etc. untouched)
- [x] [T-0219](T-0219)'s P-7 generator-resolvability gate passes on all 9 sidecars
- [x] Existing `visibility-sweep` gate passes on all 9 PNGs
- [x] Existing `provenance-sweep` (`model_hash` non-null) gate still passes on all 9 (unaffected by this change, but re-confirm)
- [x] Full `tools/asset-gate` pytest suite green, ruff clean
- [ ] Commit + open a PR. Do **NOT** merge.

## Notes

- This is the entity-sheet sibling of [T-0221](T-0221) (which remediates the 5 signal_tower props) — together they clear the 14 pre-existing failures [T-0219](T-0219) (PR #234) correctly flags, so option A (real remediation, no baseline exemptions) holds for both asset families.
- Does **NOT** depend on [T-0220](T-0220) (committed RGBA-cutout path) — these are procedural/SDXL-img2img sheets generated by the already-committed `gen_entities_v2.js`, not the prop-cutout pipeline T-0220 builds. If investigation during this card reveals gen_entities_v2.js is not actually reproducible/real after all, stop and escalate rather than quietly falling back to a T-0220-style regeneration — that would change this card's scope and dependencies.
- Leave `ready`; do not start/run — the user is choosing what runs next.

## Validation: FAIL (2026-08-23T12:41:53.876Z)

Provenance fix itself is correct — all 9 entity-v2 sidecars have generator field stripped to the bare path, no other fields changed, 156/156 pytest passed, ruff clean, TDD order and commit trailers compliant. Two failures: (1) `node tools/board/scripts/checkDeliverable.js T-0222 --require-artifact` exited 1 — tasks/T-0222.md is not committed to the branch (tasks/ is gitignored, needs `git add -f tasks/T-0222.md`); (2) acceptance criterion 8 (commit + open PR) unmet — branch `feature/T-0222` is not pushed to origin and no PR exists. Fix: force-add the task file, push the branch, and open a PR targeting develop.

(run 1 of 5)

## Recovered (2026-09-03T17:01:04.993Z)

run did not complete (board restarted or process ended before a verdict); reset to blocked for re-run.
