---
id: "T-0042"
title: "Migrations (plain SQL + version table, no ORM)"
status: "done"
priority: "P0"
phase: 4
agent: "server"
depends_on: ["T-0040", "T-0041"]
created: "2026-08-01"
---

## Context

Migration runner using plain SQL files plus a version-tracking table - no
ORM, per `docs/PLAN.md` ground rules. Implements the full revised schema
from `docs/design/04-data-model.md`: identity, session_lease, archetype,
anchor_tag, variant, item_instance (with the INV-1 CHECK constraint),
offering, unlock, vocabulary, run_archetypes, and the revised notes/ratings
tables. This is the schema landing point for the entire GDD reconciliation
- get `04-data-model.md` right here before anything else in Phase 4.

## Acceptance

- [ ] Migrations apply cleanly to a fresh database
- [ ] Test: up/down migrations are idempotent
- [ ] `item_instance.CHECK (num_nonnulls(holder, anchor_arch) = 1)` present
      and verified to reject invalid rows (INV-1)
- [ ] Schema matches `docs/design/04-data-model.md` exactly, not the
      superseded Phase-0 stub
