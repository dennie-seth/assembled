---
id: "T-0129"
title: "Multi-universe identity (sim)"
status: "done"
priority: "P0"
phase: 4
agent: "server"
depends_on: []
created: "2026-08-04"
branch: "feature/T-0129"
commit: "b8d721440063f03c94b37e63fdf6f9f233a70012"
pr: "https://github.com/dennie-seth/assembled/pull/86"
deliverable_type: "code"
attempts: 0
comments: []
attachments: []
---

## Context

Round 2 of the T-0099 economy simulation (HANDOFF Section 11.5) could not
answer whether chain-key persistence should be scoped per-run or per-week,
because the sim model has no concept of an identity that outlives a single
run. `01` Section 5 describes collapse as an event an **identity** survives
by starting a new **universe** -- the current sim conflates the two, so
there is nothing in the model that persists across a collapse boundary.
This card is a prerequisite for T-0130 (exit condition), which needs a
persistent identity to attach completion state to.

## Story

As a simulation author, I want the sim model to separate a persistent
**identity** (carries vocabulary + unlock state, survives collapse and
starts a new universe) from a **run** (one universe), so that per-run vs
per-week unlock scoping become distinguishable and later cards can model
identity-level state across collapses.

## Acceptance

- [ ] An identity survives collapse and starts a new universe within the
      same simulation run -- collapse no longer terminates the identity
- [ ] Vocabulary and unlock state are modelled as identity-level state,
      distinct from universe/run-level state
- [ ] Per-run-scoped and per-week-scoped unlock decay become
      distinguishable configurations in the sim, where previously they
      were indistinguishable

## Validation: FAIL (2026-08-05T11:41:13.115Z)

Two issues: (1) Implementation is NOT committed — config.py, engine.py, types.py changes exist only as unstaged working-tree modifications; the sole commit (eeadfe9) contains only test files (RED phase). The branch has no reviewable implementation artifact. The implementer must commit the green-phase implementation with the Co-authored-by trailer. (2) ruff reports I001 (unsorted import block) at tests/test_identity.py:16 — fixable with --fix but must be clean before review passes. pytest 123/123 green (with uncommitted changes applied). No UGC surface added, TDD ordering correct (tests committed before impl), branch naming OK.

## Validation: FAIL (2026-08-05T15:05:18.280Z)

ruff reports I001 (unsorted import block) at tests/test_identity.py:16 — the `from __future__ import annotations` block through line 23 is not sorted/formatted per ruff's isort rules. This was cited in the prior FAIL verdict and the green-phase commit message (0e26dc0) claims 'fixes ruff I001' but the error is still present. pytest 123/123 green. TDD ordering correct (eeadfe9 = test-only red phase, 0e26dc0 = implementation green phase). Both commits carry the Co-authored-by trailer. No free-text UGC surface added. Branch naming OK. Fix: run `ruff check --fix tests/test_identity.py` and commit the result.

## Validation: FAIL (2026-08-05T15:39:11.417Z)

ruff reports I001 (unsorted/unformatted import block) at tests/test_identity.py:16:1 — the import block spanning lines 16–20 (from __future__ import annotations through from sim.types import ...) is still flagged as un-sorted. Commit 5b20799 claims to fix this but the error persists. This is the third consecutive FAIL for the same issue. Fix: run `ruff check --fix tests/test_identity.py` and commit the result. All other checks pass: pytest 123/123 green, TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN), Co-authored-by trailers present on all 3 commits, no UGC surface added, branch naming OK, implementation cleanly separates identity-level state from run-scoped state.

## Validation: FAIL (2026-08-05T21:28:06.311Z)

ruff check still reports I001 (unsorted/unformatted import block) at tests/test_identity.py:16:1. The import block (lines 16–21) groups `from __future__` (stdlib), `from conftest` (third-party by ruff's classification), and `from sim.*` (first-party) with blank-line separators between each, but ruff still flags the block as un-sorted. This is the fourth consecutive FAIL for the same issue. The blank line at line 17 between `from __future__` and `from conftest` is apparently not sufficient — ruff likely wants `from __future__` in its own section with no following blank line before other stdlib, or the section ordering itself is wrong. Fix: run `ruff check --fix tests/test_identity.py` to let ruff auto-sort the block, verify the result, and commit. All other checks pass: pytest 123/123 green, TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN), Co-authored-by trailers present on all 4 commits, no free-text UGC surface added, branch naming OK, implementation cleanly separates identity-level state (vocabulary, universe_count) from run-scoped state.

## Validation: FAIL (2026-08-05T22:00:18.141Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1 — the import block spanning lines 16–20 (from __future__ import annotations, blank line, from conftest ..., from sim.engine ..., from sim.types ...) is still flagged. This is the fifth consecutive FAIL for the same issue. Three fix-attempt commits (5b20799, c8cbeca, 3f8b9b9) each claim to fix I001 but the error persists. Fix: run `.venv/bin/ruff check --fix tests/test_identity.py`, verify the output is clean with `.venv/bin/ruff check .`, then commit the result. All other checks pass: pytest 123/123 green, TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN), Co-authored-by trailers present on all 5 commits, no free-text UGC surface added, branch naming OK, implementation cleanly separates identity-level state (vocabulary, universe_count) from run-scoped state.

## Validation: FAIL (2026-08-06T12:53:04.666Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1 — the import block spanning lines 16–20 (from __future__ import annotations, blank line, from conftest ..., from sim.engine ..., from sim.types ...) is still flagged. This is the sixth consecutive FAIL for the same issue. Four fix-attempt commits (5b20799, c8cbeca, 3f8b9b9, 6d2e62a) each claim to fix I001 but the error persists. The root cause is likely the blank line at line 17 between `from __future__` and `from conftest` — ruff wants these in properly separated isort sections, or the `known-first-party` / `known-third-party` config in pyproject.toml is not correctly classifying `conftest`. Fix: run `.venv/bin/ruff check --fix tests/test_identity.py`, verify clean with `.venv/bin/ruff check .`, inspect the actual result (don't assume it worked), and commit. pytest 123/123 green. TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN). Co-authored-by trailers present on all commits. No free-text UGC surface added. Branch naming OK. Implementation cleanly separates identity-level state (vocabulary, universe_count) from run-scoped state. All three acceptance criteria would be met if lint were clean.

(run 1 of 5)

## Validation: FAIL (2026-08-06T13:30:41.494Z)

ruff check fails with a TOML parse error: pyproject.toml:37 has `src = ["src", "tests"]` under `[tool.ruff.lint.isort]`, but `src` is not a valid field for that section in ruff 0.14.4 (valid fields include `known-first-party`, `known-third-party`, `sections`, etc.). ruff cannot parse the config at all, so no lint checking occurs. This is the 7th consecutive FAIL for the lint issue — prior attempts tried adding `src` to isort config, adding `known-first-party`, and reformatting imports, but the current committed config is invalid TOML for ruff. Fix: remove the `src = ["src", "tests"]` line from `[tool.ruff.lint.isort]` (the top-level `[tool.ruff] src` already covers source root detection), then run `.venv/bin/ruff check .` to confirm clean, and if I001 persists at test_identity.py:16, run `.venv/bin/ruff check --fix tests/test_identity.py` and inspect the result before committing. pytest 123/123 green. TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN). Co-authored-by trailers present on all commits. No UGC surface added. Branch naming OK. All three acceptance criteria are functionally met pending clean lint.

(run 2 of 5)

## Validation: FAIL (2026-08-06T13:34:54.488Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1 — the import block spanning lines 16–20 (from __future__ import annotations, blank line, from conftest ..., from sim.engine ..., from sim.types ...) is still flagged. This is the 8th consecutive FAIL for the same lint issue. Eight fix-attempt commits have tried config changes (known-first-party, src roots) but none have actually run `ruff check --fix tests/test_identity.py` and committed the result — the file's import block itself has never been modified since its initial commit (eeadfe9). The fix is straightforward: run `.venv/bin/ruff check --fix tests/test_identity.py`, verify clean with `.venv/bin/ruff check .`, inspect the actual diff to confirm the blank line at line 17 was removed (or imports reordered), and commit the result. Do NOT add more pyproject.toml config — the config is fine now; the file just needs the auto-fix applied. pytest 123/123 green. TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN). Co-authored-by trailers present on all commits. No free-text UGC surface added. Branch naming OK. All three acceptance criteria are functionally met (identity survives collapse, vocabulary is identity-level, per_run vs per_week are distinguishable) — the sole blocker is this lint error.

(run 3 of 5)

## Validation: FAIL (2026-08-06T13:47:14.027Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1 — the blank line at line 17 between `from __future__ import annotations` and `from conftest import FAR_FUTURE, make_cfg` is still present and still flagged. This is the 9th consecutive FAIL for the same issue. All 6 fix-attempt commits modified pyproject.toml config but none ever changed the import block in test_identity.py itself. Fix: run `.venv/bin/ruff check --fix tests/test_identity.py`, verify the file was actually modified (the blank line at line 17 should be removed or imports reordered), confirm clean with `.venv/bin/ruff check .`, and commit the file change. Do NOT add more pyproject.toml config — the config is correct now; the file's import block just needs the auto-fix applied to its actual source. pytest 123/123 green. TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN). Co-authored-by trailers present on all commits. No free-text UGC surface added. Branch naming OK. All three acceptance criteria are functionally met pending clean lint.

(run 4 of 5)

## Validation: FAIL (2026-08-06T13:56:07.005Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1. The import block on lines 16–21 places a blank line (line 20) between `from sim.types import ...` and `from conftest import ...`, but ruff classifies both as first-party (via known-first-party config) and wants them in one contiguous block with no blank-line separator. This is the 10th consecutive FAIL for this same issue. Every prior fix attempt modified pyproject.toml config; the config is now correct, but the file itself still has the wrong import layout. Fix: run `.venv/bin/ruff check --fix tests/test_identity.py`, verify the blank line at line 20 was removed with `.venv/bin/ruff check .`, and commit the one-line file change. Do NOT modify pyproject.toml again. All other checks pass: pytest 123/123 green, TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN), Co-authored-by trailers present, no free-text UGC surface added, branch naming OK. All three acceptance criteria are functionally met pending clean lint.

(run 5 of 5) Auto-retry limit reached -- blocked for human review.

## Validation: FAIL (2026-08-06T18:49:31.506Z)

ruff check reports I001 (un-sorted/un-formatted import block) at tests/test_identity.py:16:1 — the double blank line between the last import (line 20) and the '# Helpers' comment (line 23) is treated by ruff as part of the import block formatting. `ruff check --fix --diff` confirms the fix is simply removing one blank line (line 21). This is the 11th consecutive FAIL for the same issue. All prior fix-attempt commits (5b20799, c8cbeca, 3f8b9b9, 6d2e62a, 7f5e0bf, 4d79ef3, 3c41390, 46af125) modified pyproject.toml config or reordered imports but never fixed the actual formatting issue — the extra blank line at line 21. Fix: run `.venv/bin/ruff check --fix tests/test_identity.py` (this will remove the extra blank line at line 21), verify clean with `.venv/bin/ruff check .`, inspect the diff to confirm the change, then commit the one-line deletion. Do NOT modify pyproject.toml — the config is correct. All three acceptance criteria are functionally met and all other checks pass (pytest 123/123 green, TDD ordering correct, Co-authored-by trailers present, no UGC surface, branch naming OK). The sole blocker is this formatting issue in the import block.

(run 1 of 5)

## Validation: PASS (2026-08-06T18:52:03.639Z)

pytest 123/123 green, ruff check clean (the 11-FAIL lint streak is finally resolved). TDD ordering correct (eeadfe9 RED before 0e26dc0 GREEN). Co-authored-by trailers present on all 11 commits. No free-text UGC surface added. Branch naming OK. AC-1 met: _start_new_universe replaces the QUIT path — agent survives collapse, universe_count increments, population unchanged, run stats reset, items scattered (6 tests). AC-2 met: vocabulary is a set[int] on Agent, populated on pickup, never cleared on collapse, additive across universes (3 tests). AC-3 met: unlock_scope config distinguishes per_run (clears tactical+session on collapse) from per_week (preserves all tiers); unique_keyed survives in both scopes; test_per_run_and_per_week_differ_on_tactical_survival directly asserts different outcomes (7 tests).

## PR (2026-08-06T18:52:03.639Z)

https://github.com/dennie-seth/assembled/pull/86
